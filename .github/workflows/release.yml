name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag, for example v1.6.9'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    name: Build macOS app and publish release
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve release tag
        id: release
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            tag="${{ inputs.tag }}"
          else
            tag="${GITHUB_REF_NAME}"
          fi
          if [[ -z "$tag" || ! "$tag" =~ ^v[0-9]+(\.[0-9]+)*([-.][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid release tag: $tag" >&2
            exit 1
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "artifact_name=QuickRecorder-$tag" >> "$GITHUB_OUTPUT"

      - name: Select Xcode
        shell: bash
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version

      - name: Resolve Swift packages
        shell: bash
        run: |
          xcodebuild -resolvePackageDependencies \
            -project QuickRecorder.xcodeproj \
            -scheme QuickRecorder

      - name: Build Release app
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild \
            -project QuickRecorder.xcodeproj \
            -scheme QuickRecorder \
            -configuration Release \
            -derivedDataPath build/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package app
        shell: bash
        run: |
          set -euo pipefail
          app_path="build/DerivedData/Build/Products/Release/QuickRecorder.app"
          artifact="${{ steps.release.outputs.artifact_name }}"
          test -d "$app_path"
          mkdir -p dist
          ditto -c -k --sequesterRsrc --keepParent "$app_path" "dist/$artifact.zip"
          shasum -a 256 "dist/$artifact.zip" > "dist/$artifact.zip.sha256"
          ls -lh dist

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.release.outputs.artifact_name }}
          path: |
            dist/${{ steps.release.outputs.artifact_name }}.zip
            dist/${{ steps.release.outputs.artifact_name }}.zip.sha256

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ steps.release.outputs.tag }}"
          artifact="${{ steps.release.outputs.artifact_name }}"
          if ! git rev-parse "$tag" >/dev/null 2>&1; then
            git tag "$tag" "$GITHUB_SHA"
            git push origin "$tag"
          fi
          gh release create "$tag" \
            "dist/$artifact.zip" \
            "dist/$artifact.zip.sha256" \
            --title "$tag" \
            --notes "Automated release for $tag" \
            --verify-tag
